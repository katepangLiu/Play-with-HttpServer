##
##  Makefile -- Build procedure for sample compat Apache module
##  Autogenerated via ``apxs -n compat -g''.
##

builddir=.
apxs_builddir=$(shell apxs -q installbuilddir)
top_builddir=$(apxs_builddir)/..
include $(apxs_builddir)/special.mk

#   the used tools
APXS=apxs
APACHECTL=apachectl

#   additional defines, includes and libraries
#DEFS=-Dmy_define=my_value
#INCLUDES=-Imy/include/dir
#LIBS=-Lmy/lib/dir -lmylib

#   the default target
all: local-shared-build

#   install the shared object file into Apache 
install: install-modules-yes

#   cleanup
clean:
	-rm -f mod_compat.o mod_compat.lo mod_compat.slo mod_compat.la 

#   simple test
test: reload
	lynx -mime_header http://localhost/compat

#   install and activate shared object by reloading Apache to
#   force a reload of the shared object file
reload: install restart

#   the general Apache start/restart/stop
#   procedures
start:
	$(APACHECTL) start
restart:
	$(APACHECTL) restart
stop:
	$(APACHECTL) stop

